{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".next/**"]
    },
    "types:check": {},
    "watch": {
      "cache": false
    },
    "clean": {
      "cache": false
    }
  }
}
